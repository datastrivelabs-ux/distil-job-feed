# Copy to .env.local for local development

# Neon DB — same connection string as in the Distil project
DATABASE_URL=postgresql://...

# Adzuna API (free tier: ~250 req/month) — only needed for refresh-adzuna script
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
